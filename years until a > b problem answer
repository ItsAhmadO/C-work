#include <bits/stdc++.h>
using namespace std;

string merge(string first, string last);

int main() {
    int a,b;
    cin >> a >> b; // a <= b
    int years = 0;
    
    while(a <= b){
        a*=3;
        b*=2;
        years ++;
    }

    cout << years;
    return 0;
}
